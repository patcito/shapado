!!! Strict
%html{ "xml:lang" => I18n.locale, :lang => I18n.locale, :xmlns => "http://www.w3.org/1999/xhtml" }
  %head
    %meta{ :content => "text/html; charset=utf-8", "http-equiv" => "Content-Type" }
  %body
    =yield
    %br
    %p{:style=>"border-top: 1px solid #999"}
      If you do not want to receive automatic notifications from
      = @domain
      anymore, change your preferences in the notifications screen.
      = url_for(:host => @domain, :controller => "users", :action => "show", :id => @user)
