
%p
  == #{h(@user.login)}さん、
  %br
  %br
  = h(@comment.user.login)
  さんが以下の質問にコメントしました。
  %br
  = link_to h(@question.title), url_for(:host => @group.domain, :controller => "questions", :action => "show", :id => @question)
  %br
  コメント内容：
  %br
  = find_and_preserve("<pre style='font-size:20px'>"+ shapado_auto_link(markdown(@comment.body, :group => @group))+"</pre>")

