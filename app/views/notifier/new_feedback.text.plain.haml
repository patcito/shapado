-if @user
  = t('.message1', :user =>h(@user.login), :email => h(@user.email))
-else
  = t('.message2', :ip => @ip)

= t('.message3', :email => h(@user.email))

= t('.message1', :subject => h(@subject))
==
= @content

