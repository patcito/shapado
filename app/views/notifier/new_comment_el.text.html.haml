
%p
  == Γεια σας #{h(@user.login)},
  %br
  %br
  = Ο/η h(@comment.user.login)
  σχολίασε:
  = find_and_preserve("<pre style='font-size:20px'>"+ shapado_auto_link(markdown(@comment.body, :group => @group))+"</pre>")
  στην ερώτηση
  = link_to h(@question.title), url_for(:host => @group.domain, :controller => "questions", :action => "show", :id => @question.slug)
