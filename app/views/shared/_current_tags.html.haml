.tags-list
  -current_tags.each do |tag|
    %span.tag
      = link_to h(tag), url_for(:language => current_languages, :tags => tag)
      = link_to "x", url_for(:language => current_languages, :tags => (current_tags - tag.to_a).join("+")), :class => "remove_tag"
