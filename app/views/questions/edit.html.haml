%h2
  Editing question
- form_for(@question, :html => {:class => "form"}) do |f|
  = f.error_messages
  =render :partial => "form", :locals => {:f => f}
  .field-group
    .buttons
      = f.submit 'Update'
      = link_to 'Cancel', @question, :class => "button cancel"
